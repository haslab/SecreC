#OPTIONS_SECREC --implicitcoercions=offc

kind additive3p;
domain private additive3p;

private int classify (public int x) {
    //stub
    private int y;
    return y;
}

private int operator + (private int x, private int y) {
    //stub
    private int ret;
    return ret;
}

void main () {
    private int [[1]] x (1);
    ++ x[0];
    ++ x;
    x[0] ++;
    x ++;
}
